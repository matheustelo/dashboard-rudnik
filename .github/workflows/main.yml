name: CI/CD - Dashboard de Vendas

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./ # raiz do projeto

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Instalar dependências
        run: npm install

      - name: Rodar testes unitários
        run: npm run test -- --passWithNoTests

      - name: Build do Frontend (Vue)
        run: npm run build

      - name: Upload da pasta dist como artefato
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar chave SSH
        env:
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf "%s" "$SSH_KEY" > ~/.ssh/rudnikKey
          chmod 600 ~/.ssh/rudnikKey
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Instalar Ansible (caso não esteja presente)
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible

      - name: Teste de conexão SSH (debug)
        run: |
          echo "Comando SSH que será executado:"
          echo "ssh -i ~/.ssh/rudnikKey -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}"
          echo "Tentando conectar..."
          ssh -i ~/.ssh/rudnikKey -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "echo '✅ Conexão SSH bem-sucedida'"

      - name: Executar Ansible Playbook
        run: |
          ansible-playbook ansible/deploy.yml \
            -i "${{ secrets.SERVER_HOST }}," \
            --private-key ~/.ssh/rudnikKey \
            -u ${{ secrets.SERVER_USER }}
